version: '3.9'

services:
  postgres:
    image: postgres:15-alpine
    container_name: kabar-postgres-staging
    env_file:
      - ../apps/api/.env.production
    ports:
      - "5432:5432"
    volumes:
      - postgres_staging_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - kabar-staging

  redis:
    image: redis:7-alpine
    container_name: kabar-redis-staging
    env_file:
      - ../apps/api/.env.production
    command: sh -c "redis-server --requirepass $$REDIS_PASSWORD"
    ports:
      - "6379:6379"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - kabar-staging

  api:
    build:
      context: ../apps/api
      dockerfile: Dockerfile
    container_name: kabar-api-staging
    env_file:
      - ../apps/api/.env.production
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - kabar-staging

  web:
    build:
      context: ../apps/web
      dockerfile: Dockerfile
      args:
        VITE_API_URL: /api
    container_name: kabar-web-staging
    depends_on:
      - api
    restart: unless-stopped
    networks:
      - kabar-staging

  nginx:
    image: nginx:alpine
    container_name: kabar-nginx-staging
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - nginx_staging_logs:/var/log/nginx
    depends_on:
      - api
      - web
    restart: unless-stopped
    networks:
      - kabar-staging

volumes:
  postgres_staging_data:
  nginx_staging_logs:

networks:
  kabar-staging:
    driver: bridge